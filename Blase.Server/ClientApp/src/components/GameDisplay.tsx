import './GameDisplay.css';

import { GameUpdate } from "../data/GameUpdate";

export function GameDisplay(props: { update: GameUpdate }) {
    const evt = props.update;

    return (
        <div>
            
        </div> 
    )
}